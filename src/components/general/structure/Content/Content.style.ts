import { StyleSheet } from 'react-native';

export const useContentStyles = () =>
  StyleSheet.create({
    content: {
      width: '100%',
      alignItems: 'center',
      paddingHorizontal: 30,
    },
  });

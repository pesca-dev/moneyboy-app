import { StyleSheet } from 'react-native';

export const useListItemStyles = () => {
  return StyleSheet.create({
    listItem: {
      flexDirection: 'row',
      paddingVertical: 10,
    },
  });
};
